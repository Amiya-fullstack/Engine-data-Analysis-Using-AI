class GeneratorAgent:
    """Generates responses or artifacts based on templates and retrieved data."""

    def generate(self, context: dict):
        return {"content": "generated", "context": context}
